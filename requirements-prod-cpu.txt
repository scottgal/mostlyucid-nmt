# requirements-prod-cpu.txt
# Production dependencies for CPU-only builds
#
# Uses CTranslate2 instead of PyTorch for ~10x smaller package size
# CTranslate2 CPU: ~20MB vs PyTorch CPU: ~200MB

fastapi>=0.109.0
pydantic>=2.0.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0

# CTranslate2 - Fast inference engine (replaces PyTorch)
ctranslate2>=4.0.0

# Tokenizers (still needed, but NOT for inference)
transformers>=4.36.0
huggingface_hub>=0.20.0
sentencepiece>=0.1.99
sacremoses>=0.0.53
protobuf>=3.20.0
accelerate>=0.26.0  # Required for HY-MT and MADLAD model families

# Utilities
langdetect>=1.0.9
tqdm>=4.66.0
psutil>=5.9.0
httpx>=0.26.0
