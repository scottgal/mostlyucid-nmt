# requirements.txt
# Core dependencies
fastapi>=0.109.0
pydantic>=2.0.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0

# CTranslate2 - Fast inference engine (replaces PyTorch for inference)
ctranslate2>=4.0.0

# Tokenizers and model loading (still needed for tokenization)
transformers>=4.36.0
huggingface_hub>=0.20.0
sentencepiece>=0.1.99
sacremoses>=0.0.53
protobuf>=3.20.0

# PyTorch - OPTIONAL, only needed for transformers backend or model conversion
# Uncomment if using TRANSLATION_BACKEND=transformers or for first-time model conversion
# For CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch>=2.0.0

# Language detection
langdetect>=1.0.9

# HTTP and utilities
httpx>=0.26.0
tqdm>=4.66.0
psutil>=5.9.0

# MCP (Model Context Protocol) for LLM tool integration
mcp>=1.0.0

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
